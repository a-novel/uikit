import { Metadata } from "next";

import { PageComponent } from "./page-component";

export const metadata: Metadata = {
  title: "UI Navigation Layouts",
};

const Page = () => <PageComponent />;

export default Page;
