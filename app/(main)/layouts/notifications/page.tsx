import { Metadata } from "next";
import { PageComponent } from "./page-component";

export const metadata: Metadata = {
  title: "UI Layouts Notifications",
};

const Page = () => <PageComponent />;

export default Page;
